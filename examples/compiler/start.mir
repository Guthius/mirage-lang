const std := import("std")
const io := import("io")
const mem := import("memory")

pub fn main() -> error {
    const f_len, f_bytes := try io.load_file("Justfile")
    defer mem.free(f_bytes)

    io.printf("%.*s\n", f_len, f_bytes)
    return 0
}
